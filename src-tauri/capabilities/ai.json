{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "ai",
  "description": "Capabilities for AI client scenarios: HTTP access to LLM providers.",
  "windows": ["main"],
  "permissions": [
    "http:default"
  ]
}
