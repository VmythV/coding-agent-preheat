{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "tools",
  "description": "Capabilities for local tool scenarios: filesystem access and shell execution.",
  "windows": ["main"],
  "permissions": [
    "fs:default",
    "fs:allow-read-text-file",
    "fs:allow-write-text-file",
    "fs:allow-exists",
    "shell:default"
  ]
}
